<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.groupl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="MealPlanner"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AllPlanAdapter"></activity>
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShowAllRecipes" />
        <activity android:name=".AddNewRecipe" />
        <activity android:name=".EditRecipe" />
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Addaplan" />
    </application>

</manifest>